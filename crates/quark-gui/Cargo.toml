[package]
name = "quark-gui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Shared GUI protocol, catalog, and session reducer for Quark Downloader"
publish = false

[lib]
name = "quark_gui"
path = "src/lib.rs"
crate-type = ["rlib", "staticlib"]

[dependencies]
quark-core = { path = "../quark-core" }
