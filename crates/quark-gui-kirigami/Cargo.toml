[package]
name = "quark-gui-kirigami"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Quark Downloader Kirigami frontend (compiled into quark-downloader-gui)"
publish = false

[lib]
name = "quark_gui_kirigami"
path = "src/lib.rs"

[dependencies]
quark-gui = { path = "../quark-gui" }

[build-dependencies]
cc = "1"
