[package]
name = "quark-gui-kirigami"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Quark Downloader Kirigami frontend (system Qt 6 / Kirigami QML)"
publish = false

[[bin]]
name = "quark-downloader-gui-kirigami"
path = "src/main.rs"

[dependencies]
quark-gui = { path = "../quark-gui" }
